<div class="show">
<h3>Title:</h3>
<p class="shows"><%= @book.title %></p>
</div>

<div class="show">
<h3>Body:</h3>
<p class="shows"><%= @book.body %></p>
</div>

<%= link_to "Edit", edit_book_path(@book.id) %>
<div class="border">|</div>
<%= link_to "Back", books_path(@book.id) %>
<% if flash[:notice] %>
     <p id="notice">
       <%= flash[:notice] %>
     </p>
   <% end %>